@model IEnumerable<AaronOutdoors.Models.BlogView>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BlogViewId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BlogViewId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BlogViewId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
